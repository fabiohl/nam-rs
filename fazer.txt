* /documentador: Atualizar criticamente o "docs/architecture.md" para o estado atual da arquitetura, recentemente revisada como um todo. Atualizado, limpo, útil!
  Buscar não ser repetitivo com nada do restante do diretório "doc/". Deixe que cada documento cumpra o seu papel especializado. Ele ("docs/architecture.md") é apenas o "diretório geral". E o que não cabe em nenhum outro documento.
  Fazer a mesma coisa com o "docs/dependencies.md". Ele é muito repetitivo com o "README.md" e com o próprio "Cargo.toml"! Idealmente, este arquivo deveria deixar de existir, com suas informações sendo absorvidas por outros documentos.
  Finamente, ataque o "docs/troubleshooting.md". Procedimentos simples de troubleshooting (pelo usuário final) cabem ao "README.md", a skills em ".agents/" ou a outros lugares.
  E tudo isto feito, atualizar também o ".agents/rules/rust.md". Muito cuidado com este. Ele deve ser enxuto e muito direto. Não ser de forma alguma repetitivo com nada em "docs/*.md". Contém apenas o absoluto essencial - os detalhes estão em "docs/*.md".
  Depois disto tudo, cuidado para não aglomerar informações. Os documentos final todos DEVEM ser sintético, fáceis de entender, resumidos, diretos ao ponto, útil e verdadeiro.

* Lidar com estes bugs: known-bugs.md
* Prova de conceito de impacto (performance e qualidade) da remoção de:
  docs/audio_fidelity_map.md "1. Weight Compression — F16C / BF16"; "3. LSTM Recurrent State Quantization Drift".
  docs/cpp_parity_map.md "2.5 Precision divergence: quantized backbone weights", "2.7 Measured interop drift" e "7.2 🟡 Known, measured, accepted tradeoffs — not bugs" / LSTM backbone weight quantization.
  docs/lstm_recurrent_drift.md

* /refatora-rust [GLM5.2]
* /revisor-auditor [GLM5.2][Fable] totalmente focado na role "Resilience and Robustness Specialist".
* /revisor-auditor [GLM5.2][Fable] totalmente focado na role "Performance and Low-Latency Master".
  Analise meticulosamente os logs dos testes registrados em `testes.log` atrás de insights ocultos nas entrelinhas!
  Analise o "target/dsp_hotpath.asm" gerado pelo "utils/build-release.sh" atrás de insights de performance!
  Inclua no workflow, rodadas regulares de "tests-performance-regression.sh" para sempre assegurar ganhos reais.
  Focar exclusivamente no baseline x86-64-v3. Explore-o ao máximo, mega otimizado e universal em TODO o código (não apenas no áudio). Outros ISA's multiversioning serão trabalhados oportunamente, não são foco aqui.
  É terminantemente PROIBIDO qualquer regressão de paridade com o NAMcore ou qualquer penalização da fidelidade e correção sonora.
  Analise também se o NAM-rs está tirando extremo proveito das funcionalidades disponibilizada pela stack moderna do Kernel Linux (7.0), CLAP (1.2.x) e Pipewire (1.6.x). Naturalmente, aplique um filtro crítico para identificar apenas o que realmente pode trazer benefícios palpáveis ao caso de uso realista NAM-rs.
  Ao final, como complemento, ativar a skill "pesquisador-inovador" e ir além. Mas tenha muito cuidado para não alterar o escopo do projeto e trazer complexidade. Tire proveito do que já temos.
