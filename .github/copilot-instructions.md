# AI Agents Context & Rules

Always read the rules in `.agents/rules/` and adopt the personas from `.agents/skills/` before starting any task. Do not proceed with code generation until you have read the relevant guidelines.
